export enum Strategy {
  None,
  Breakout,
  MovingAverages,
}

export enum BreakoutPeriod {
  Minute,
  TenMinutes,
  Hour,
  Day,
}

export enum Side {
  Buy,
  Sell
}


export enum StopLossStrategy {
  Breakout,
  CustomLimit
}

