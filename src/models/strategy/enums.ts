export enum Strategy {
  Breakout,
  MovingAverages,
  // Add other strategies here
}

export enum BreakoutPeriod {
  Minute,
  TenMinutes,
  Hour,
  Day,
}

export enum Side {
  Buy,
  Sell
}


export enum StopLossStrategy {
  Breakout,
  CustomLimit
}

